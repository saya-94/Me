<%= render 'common_template/admin_header' %>

<div class="container">
    <div class="row">

    <%# サイドメニュー & genre追加form ここから %>
        <div class="col-md-3">
            <%= render 'common_template/admin_box' %>
        </div>
    <%# サイドメニュー & genre追加form ここまで %>

        <div class="col-md-7 col-md-offset-1">
            <h2>ジャンル</h2>

            <table class="table table-striped table-responsive">
                <thead>
                    <tr>
                    <th><strong>ジャンル名</strong></th>
                    <th></th>
                    <th></th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td><%= @genre.genre %></td>
                        <td class="text-center"><%= link_to "編集", edit_genre_path, class: "btn btn-primary" %></td>
                        <td class="text-center"><%= link_to "削除", genre_path, method: :delete , class: "btn btn-danger", data: { confirm: "本当に削除しますか？"}%></td>
                    </tr>
                </tbody>
            </table>

        </div>
        <%# <div class="col-md-2"></div> %>
    </div>
</div>

<%= render 'common_template/footer' %>